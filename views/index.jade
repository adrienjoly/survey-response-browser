extends layout

block content
  each section,key in sections
    section
      .content
        .text
          h2=section.title
          p=section.text
          a.button(href=section.cta_link)=section.cta
      div(style="clear:both;")
