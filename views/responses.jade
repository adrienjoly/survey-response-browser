extends layout

block content
  each submission in responses
    section
      each response in submission
        h2=response.key
        each line in (response.value || '').split('\n')
          p=line
  p made with https://github.com/adrienjoly/survey-response-browser